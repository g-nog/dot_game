# Generated game artwork

Generated with the built-in `image_gen` tool, using the imagegen skill, on 2026-09-05. Both selected images were visually reviewed and copied into the repository. Neither game depends on files outside the project. No CLI/API fallback was used.

## Celestial atlas

Saved asset: `public/images/celestial-atlas.png` (1536 × 1024 PNG).

Used as the constellation page scenery, board background, and gallery preview. Bright playable stars, graph connections, legal endpoint rings, and completion motifs are rendered from the actual logical state, not baked into the image.

Exact generation prompt:

```text
Use case: stylized-concept
Asset type: background artwork for a celestial-atlas local constellation strategy game and its gallery card.
Primary request: an exquisite deep navy celestial atlas of space with faint teal and violet nebula wisps, delicate gold astronomical engraving at the outer corners, tiny distant starlight. Wide landscape composition, large dark quiet central region for a playable star graph overlay. Refined observatory atmosphere, subtle depth, restrained luminosity, tactile fine-grain print detail.
Constraints: no text, no letters, no UI, no large foreground stars, no lines connecting stars, no watermark. Background must stay dark and understated so luminous interactive stars and colored connections remain clearly readable.
```

## Triangle Duel preview

Saved asset: `public/images/triangle-duel.png` (1536 × 1024 PNG).

Used only in the gallery card, retaining the existing game's visual identity and game screen.

Exact generation prompt:

```text
Use case: stylized-concept
Asset type: game gallery preview artwork for Triangle Duel, a local strategy game about connecting dots to claim triangles.
Primary request: elegant abstract geometric tabletop, a handful of thin warm ivory straight lines joining small round nodes into irregular adjacent triangles on matte charcoal navy. A few triangular faces glow translucent coral and seafoam teal; a small ivory six-sided die rests near the lower edge. Orthographic overhead view, refined tactile paper and frosted glass materials, understated studio illumination, balanced composition with generous margin. Wide landscape.
Constraints: no lettering, no UI, no watermark, no people.
```
